add_namespace = super_events

country_event = {
	title = super_events.1.t #Great Patriotic War starts
	desc = super_events.1.d
	id = super_events.1
	#picture = COUNTRY_hoi4tgw_report_austria_hungary
	
	fire_only_once = yes
	is_triggered_only = yes

	immediate = {
		hidden_effect = {
			every_country = { 
				limit = { 
					check_variable = { super_events_enabled > 0 }
				}
				scoped_sound_effect = "ic_great_patriotic_war_starts"
			}
		}
		set_global_flag = ic_flag_great_patriotic_war_starts
	}

	option = {
		name = super_events.1.a
	}
}